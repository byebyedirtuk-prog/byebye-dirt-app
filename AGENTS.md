# Bye Bye Dirt Cleaning

Use `docs/ByeByeDirt_App_Specification_for_Codex.md` as the source of truth for this project.

If code conflicts with that specification, the specification wins. Do not implement modules listed as outside Phase 1. When a business detail is missing, mark it explicitly as `não especificado` instead of inventing a rule.
